/* Hero section style */
#hero {
  height: 80vh;
  background: url('../visuals/class_distribution.png') center/cover no-repeat;
  display: flex; justify-content: center; align-items: center;
}
#hero h1 {
  font-size: 3rem;
  font-weight: bold;
}
#hero p {
  font-size: 1.25rem;
  margin-bottom: 1.5rem;
}

/* Smooth scrolling */
html {
  scroll-behavior: smooth;
}

/* Dark mode support */
@media (prefers-color-scheme: dark) {
  body { background-color: #121212; color: #ddd; }
  #about, #code { color: #ddd; }
  .bg-light { background-color: #1e1e1e !important; }
  footer, .navbar { background-color: #1a1a1a !important; }
}
