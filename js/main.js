// main.js - orchestrator (keeps minimal, delegated to ui/storage/analytics)
(function(){
  // simple exposure to console for debugging
  window.dc = {
    store: window.DataStore,
    analytics: window.Analytics
  };
  // When DOM ready, rendered by ui.js handlers
})();
